name: sakthiiiii 

on: 
  workflow_dispatch:

env: 
  MY_VAR: ${{ secrets.TEST_SECRET }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: print secert uh
        run: echo "priting my variabbel uh" $MY_VAR
